name: Test if code compiles with -Werror

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make -j$(nproc) CFLAGS="-std=c23 -Wall -Wextra -Werror"
